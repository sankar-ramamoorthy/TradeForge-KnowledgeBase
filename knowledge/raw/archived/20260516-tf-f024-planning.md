---
title: TF-F024 Planning - Credential Script Invocation Fix
type: brainstorm
status: raw
tags: [tradeforge, tf-f024, planning, credential-setup]
created: 2026-05-16
---

# TF-F024 Planning - Credential Script Invocation Fix

## Approach

- Keep the documented command form unchanged.
- Add a small direct-execution bootstrap in `scripts/manage_credentials.py` so the repository root is inserted into `sys.path` before importing `src.security`.
- Add subprocess-based regression tests that execute the documented script path for both `generate-master-key` and `register`.
- Keep `HOW-TO-SETUP-KEYS.md` unchanged unless verification reveals a mismatch.

## Rationale

This fixes the diagnosed packaging/import gap at the smallest possible scope while preserving the operator-facing workflow already documented in M10C.

## Replay / Lifecycle Impact

None.

## Regression Focus

- direct script invocation from repo root
- credential registration under the documented command style
- existing credential behavior unchanged
